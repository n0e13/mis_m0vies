include ../head.pug
h2 Reestablecer contraseña
p Establece la nueva contraseña para tu cuenta
//- -let restoreToken = window.location.href 
form#recoverToken( method="post") 
    label(for="pass1") Escribe la contraseña
    input(type="password", name="pass1") 
    label(for="pass2") Repite la contraseña
    input(type="password", name="pass2") 
    button(type="submit") Restaurar contraseña

script(src="/js/recoverToken") 
