include ../head.pug


header
        nav(class='navbar')
                                a(href="/dashboard", class="nav-branding") MovieApp
                                ul(class="nav-menu")
                                        li(class="nav-item")
                                                a(href="/dashboard", class="nav-link") Dashboard
                                        li(class="nav-item")
                                                a(href="/movies", class="nav-link") My movies
                                        li(class="nav-item")
                                                a(href="/logout", class="nav-link") Logout
                                div(class="hamburger")
                                        span(class="bar")
                                        span(class="bar")
                                        span(class="bar")

h3 ¡Search a movie! 
form(action="/search", method="post", class="searchForm") 
                        label(for="films") Type de title of a movie
                        br
                        br
                        input(type="text" name="films" placeholder="Title...")
                        button(type="submit") Search
div(class="lds-roller")
        div
        div
        div
        div
        div
        div
        div
        div
script(src="/js/hamburger.js")
script(src="/js/spinner.js") 
